By default, MySQL running on linux uses case-sensitive table names.
When modifying or generating new sql files, the table name used in the
sql statements should match the case of the @Table annotation JPA entities.